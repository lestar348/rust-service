#!/usr/bin/env bash
# Deploy the built artifacts to the Raspberry Pi target.
set -euo pipefail

# Placeholder: copy binaries, configs, and restart the systemd service.
echo "Deploying service to target..."
# scp target/armv7-unknown-linux-gnueabihf/release/service_app user@pi:/usr/local/bin/
# ssh user@pi "sudo systemctl restart service-app"
